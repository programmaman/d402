/**
 * Autosigner entry point for x402-style automatic payment flows.
 *
 * This package surface is intentionally separate from the base client API because
 * unattended payment policy, budget limits, and wallet custody have different
 * risk boundaries than ordinary user-approved payment creation.
 */
export {};
